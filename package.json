{
  "$schema": "https://json.schemastore.org/package",
  "author": "Romain Racamier-Lafon <romain.racamier@gmail.com>",
  "bugs": "https://github.com/Shuunen/logo-tester/issues",
  "dependencies": {
    "class-variance-authority": "^0.7.1",
    "clsx": "^2.1.1",
    "lucide-react": "^1.46.0",
    "radix-ui": "^1.6.7",
    "react": "^19.3.0",
    "react-dom": "^19.3.0",
    "shuutils": "^10.6.0",
    "tailwind-merge": "^3.7.0",
    "tw-animate-css": "^1.4.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0",
    "@tailwindcss/vite": "^4.3.3",
    "@testing-library/jest-dom": "^7.0.1",
    "@testing-library/react": "^16.3.3",
    "@testing-library/user-event": "^14.6.7",
    "@types/node": "^26.6.1",
    "@types/react": "^19.3.0",
    "@types/react-dom": "^19.3.0",
    "@vitejs/plugin-react": "^6.1.1",
    "@vitest/coverage-v8": "^5.0.1",
    "@vitest/ui": "^5.0.1",
    "bun": "^1.4.2",
    "happy-dom": "^20.14.5",
    "oxfmt": "^0.68.0",
    "oxlint": "1.83.0",
    "oxlint-tsgolint": "^7.0.2001",
    "tailwindcss": "^4.3.3",
    "tiny-glob": "^0.2.9",
    "turbo": "^2.10.13",
    "typescript": "~7.0.2",
    "vite": "^8.3.0",
    "vitest": "^5.0.1"
  },
  "homepage": "https://github.com/Shuunen/logo-tester",
  "keywords": [
    "logo",
    "icon",
    "branding",
    "test"
  ],
  "license": "GPL-3.0",
  "name": "@shuunen/logo-tester",
  "packageManager": "pnpm@10.30.3",
  "private": true,
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Shuunen/logo-tester.git"
  },
  "scripts": {
    "build": "turbo run build:tsc build:vite && echo build completed successfully || (echo Please fix build errors and run pnpm run build to check for remaining build issues && exit 1)",
    "build:tsc": "tsc -b && echo build:tsc completed successfully || (echo Please fix type errors and run pnpm run build:tsc to check for remaining type issues && exit 1)",
    "build:vite": "vite build && echo build:vite completed successfully || (echo Please fix build errors and run pnpm run build:vite to check for remaining build issues && exit 1)",
    "check": "turbo run --output-logs=errors-only lint:types lint:format lint:issues lint:sources build:tsc build:vite test:unit test:e2e && echo check completed successfully",
    "dev": "vite",
    "lint:format": "oxfmt --check && echo lint:format completed successfully || (echo Please run pnpm run lint:format:fix to automatically fix formatting issues && exit 1)",
    "lint:format:fix": "oxfmt --write && echo lint:format:fix completed successfully || (echo Please run pnpm run lint:format to check for remaining formatting issues && exit 1)",
    "lint:issues": "oxlint --type-aware --deny-warnings --report-unused-disable-directives && echo lint:issues completed successfully || (echo Please run pnpm run lint:issues:fix to automatically fix linting issues && exit 1)",
    "lint:issues:fix": "oxlint --fix --type-aware && echo lint:issues:fix completed successfully",
    "lint:sources": "bun src/bin/lint.cli.ts --target=src/**/*.tsx && echo lint:sources completed successfully || (echo Please fix source issues && exit 1)",
    "lint:types": "tsc -p tsconfig.app.json && tsc -p tsconfig.node.json && echo lint:types completed successfully || (echo Please fix type errors and run pnpm run lint:types to check for remaining type issues && exit 1)",
    "preview": "vite preview",
    "test": "turbo run test:unit test:e2e",
    "test:e2e": "playwright test && echo test:e2e completed successfully || (echo Please fix e2e test errors and run pnpm run test:e2e to check for remaining test issues && exit 1)",
    "test:ui": "vitest --ui",
    "test:unit": "vitest run --coverage && echo test:unit completed successfully || (echo Please fix test errors and run pnpm run test:unit to check for remaining test issues && exit 1)",
    "test:watch": "vitest",
    "up:deps": "pnpm update --latest && echo up:deps completed successfully"
  },
  "type": "module",
  "version": "1.0.0"
}
